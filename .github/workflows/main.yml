name: Basic Commands

on:
 push:
   branches:
     -main

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
       - name: show current working directory
          run:pwd
       
      
    
  
